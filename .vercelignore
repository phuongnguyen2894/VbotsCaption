# Local Ollama model files — these live on your PC, never on Vercel
*.gguf
Modelfile

# Training artifacts — not needed by the web app
captions_dataset.jsonl
train_vn_caption.ipynb

# Build/dependency output (regenerated by Vercel)
.next
node_modules
